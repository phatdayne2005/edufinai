spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_service?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8085
springdoc:
  swagger-ui:
    path: /swagger-ui.html